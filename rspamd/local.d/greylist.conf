# Greylisting configuration (requires Redis)
# Temporarily rejects unknown sender/recipient/IP triples.
# Legitimate servers retry; spammers usually don't.
enabled = true;
timeout = 5min;
expire = 1d;
whitelisted_ip = "/etc/rspamd/local.d/maps.d/greylist_whitelist_ip.map";
