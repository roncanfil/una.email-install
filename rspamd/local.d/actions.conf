# Spam action thresholds
# Emails scoring above these values will trigger the corresponding action

reject = 15;        # Reject emails with score >= 15
add_header = 6;     # Add spam header for score >= 6
greylist = 4;       # Greylist for score >= 4
